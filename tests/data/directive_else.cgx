<template>
  <widget>
    <label v-if="show_label" text="Foo" />
    <label v-else text="Bar" />
  </widget>
</template>

<script lang="python">
import collagraph as cg


class Labels(cg.Component):
    def __init__(self, props):
        super().__init__(props)
        self.state["show_label"] = props.get("show", True)
</script>
