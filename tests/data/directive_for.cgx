<template>
  <widget>
    <label v-for="idx, label in labels" :key="idx" :text="label" />
  </widget>
</template>

<script lang="python">
import collagraph as cg


class Labels(cg.Component):
    def __init__(self, props):
        super().__init__(props)
        # TODO: make tests for 'required' props
        # such as expecting 'labels' to be part of self.props
        # self.state["labels"] = props.get("labels", ["Foo", "Bar"])
</script>
