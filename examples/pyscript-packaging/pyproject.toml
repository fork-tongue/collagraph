[project]
name = "app"
version = "0.1.0"
description = ""
authors = [{ name = "Your Name", email = "<you@example.com>"}]
requires-python = ">=3.9"
readme = "README.md"
dependencies = ["collagraph>=0.5.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
